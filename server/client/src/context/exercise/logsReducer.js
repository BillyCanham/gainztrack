import {
     ADD_EXERCISE_LOG,
     DELETE_EXERCISE_LOG, 
     UPDATE_EXERCISE_LOG, 
     UPDATE_EXERCISE_LOG_NAME, 
     SET_CURRENT_LOG,
     CLEAR_CURRENT_LOG, 
     FILTER_EXERCISE_LOGS, 
     CLEAR_FILTER, 
     SET_ALERT, 
     REMOVE_ALERT, 
     
} from '../types';

export default (state, action) => {
    switch(action.type){
        // ADD_EXERCISE_LOG,
        // DELETE_EXERCISE_LOG, 
        // UPDATE_EXERCISE_LOG, 
        // UPDATE_EXERCISE_LOG_NAME

        // SET_CURRENT_LOG,
        // CLEAR_CURRENT_LOG, 
        // FILTER_EXERCISE_LOGS, 
        // SET_CURRENT_LOG
        default:
            return state;
        }
    }